<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %> 
<body>
    <header class="container-fluid headerLogin">
        <div class="row py-3">
            <div class="col-4 col-md-2 col-lg-2 col-xl-1 d-flex align-items-center"><a href="/"><img src="/images/mascoshop_logo.png" alt="" class="w-100"></a></div>
            <div class="txt d-flex align-items-center justify-content-center col-8 col-md-6 col-lg-5 col-xl-4">
                <span>¡ Todo lo que tu mejor amigo necesita <i class="fas fa-paw"></i> ! </span>
            </div>
            <div class="perritos col-3 col-lg-5 col-xl-6">
                <img src="/images/header/pet_1.png" alt="">
                <img src="/images/header/pet_2.png" alt="">
                <img src="/images/header/pet_3.png" alt="">
                <img src="/images/header/pet_4.png" alt="">
            </div>
        </div>
    </header>
    
    <main class="register container my-3 py-3">
        <ul class="d-flex justify-content-end">
            <li style="font-weight: bold; color: white;">Themes:</li>
            <li id="temaPreterminado" class="mx-2"><i class="fas fa-th-large" style="color: #1a508b;  cursor: pointer;"></i></li>
            <li id="temaOscuro" class="mx-1"><i class="fas fa-th-large" style="color: #333634; cursor: pointer;"></i></li>
            <li id="temaClaro" class="mx-2"><i class="fas fa-th-large" style="color: #1e6f5c; cursor: pointer;"></i></li>
        </ul>
        <div class="row">
            
            <div class="login col-12 col-md-6">
                <h2 class="my-3 text-center">Login</h2>
                <form id="formLogin" action="/users/login" method="POST">
                    <div class="mb-3">
                        <input id="loginEmail" type="email" name="emailLog" placeholder="Email" class="form-control"  value="<%= (typeof old != 'undefined' && old.emailLog) ? old.emailLog : ""%>">
                        <span id="loginEmailErrors" class="text-danger"><%= (typeof errores != 'undefined' && errores.emailLog) ? errores.emailLog.msg : "" %></span>
                    </div>
                    <div class="mb-3 icono-contrasenia-login">
                        <input id="loginpass" type="password" name="pass" placeholder="Contraseña" class="form-control" >
                        <i class="fas fa-eye btn" id="boton1"></i>
                        <span id="loginpassErrors" class="text-danger"><%= (typeof errores != 'undefined' && errores.pass) ? errores.pass.msg : "" %></span>
                    </div>
                    <span class="text-danger" id="submitErrorsLogin"></span>
                    <div class="olvide-contrasenia my-3">
                        <span>¿Olvidaste tu contraseña? </span><a href="/users/contraNueva">Haz click aqui</a>
                    </div>
                    <div class="mb-3 form-check">
                        <input type="checkbox" class="form-check-input" id="exampleCheck1" name="recordarme">
                        <label class="form-check-label" for="exampleCheck1">Recordarme</label>
                    </div>
                    <div class="botones">
                        <div class="boton-dos">
                            <button type="submit" class="mb-3 btn btn-primary">Iniciar sesion</button>
                        </div>
                        <a href="/" class="mb-3 btn btn-danger">Cancelar</a>
                    </div>
                    <% if (typeof error != 'undefined') { %>
                        <div class="alert alert-danger mt-5">
                            <span><%= error %> </span>
                        </div>
                        <% } %>
                    </form>
                    
                </div>
                
                <div class="col-12 col-md-6">
                    <h2 class="my-3 text-center">Registro</h2>
                    <form id="formRegister" action="/users/register" method="POST" enctype="multipart/form-data">
                        <div class="row">
                            <div class="mb-3">
                                <input type="text" id="name" name="name" placeholder="Nombre" class="form-control" value="<%= (typeof old != 'undefined' && old.name) ? old.name : ""%>" >
                                <span id="nameErrors" class="text-danger"><%= (typeof errores != 'undefined' && errores.name) ? errores.name.msg : "" %></span>
                            </div>
                            <div class="mb-3">
                                <input type="text" id="lastname" name="apellido" placeholder="Apellido" class="form-control"  value="<%= (typeof old != 'undefined' && old.apellido) ? old.apellido : ""%>" >
                                <span id="lastnameErrors" class="text-danger"><%= (typeof errores != 'undefined' && errores.apellido) ? errores.apellido.msg : "" %></span>
                            </div>
                            <div class="mb-3">
                                <input id="email" type="email" name="email" placeholder="Email" class="form-control"  value="<%= (typeof old != 'undefined' && old.email) ? old.email : ""%>">
                                <span id="emailErrors" class="text-danger"><%= (typeof errores != 'undefined' && errores.email) ? errores.email.msg : "" %></span>
                            </div>
                            <div class="mb-3 icono-contrasenia-registro">
                                <input id="pass" type="password" name="passUno" placeholder="Contraseña" class="form-control">
                                <i class="fas fa-eye btn" id="boton2"></i>
                                <span id="passErrors" class="text-danger"><%= (typeof errores != 'undefined' && errores.passUno) ? errores.passUno.msg : "" %></span>
                            </div>
                            <div  class="mb-3 icono-contrasenia-registro2">
                                <input id="pass2" type="password" name="passDos" placeholder="Repetir contraseña" class="form-control">
                                <i class="fas fa-eye btn" id="boton3"></i>
                                <span id="pass2Errors" class="text-danger"><%= (typeof errores != 'undefined' && errores.passDos) ? errores.passDos.msg : "" %></span>
                            </div>
                            <div class="mb-3">
                               <!--  <input type="file" id="avatar" name="avatar" class="form-control"> -->

                               <button id="elBontm" type="button" class="btn btn-danger mx-3" data-bs-toggle="modal" data-bs-target="#exampleModal">
                                Elegir avatar
                              </button>
                              <div id="inputAvatar"></div>
                                <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
                                    <div class="modal-dialog">
                                      <div class="modal-content">
                                        <div class="modal-header">
                                          <h5 class="modal-title" id="exampleModalLabel">Seleccione un avatar!</h5>
                                          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                        </div>
                                        <div class="modal-body">
                                            <div class="container-fluid">
                                            <div class="d-flex flex-wrap"  ><!-- estilos a cambiar -->
                                                <% imagenes.forEach(element => { %>
                                                    <a id="aImagen" href="#"><img id="imagenPredefinada" src="/images/users/<%= element.imagenAnimal %>" alt="<%= element.imagenAnimal %>"></a>       
                                                <% }) %>
                                          
                                         
                                          
                                        </div>
                                        </div>
                                        <div id="textoImagen"></div>
                                        </div>
                                        <div class="modal-footer">
                                          <button type="button" class="btn btn-success" data-bs-dismiss="modal">Cancelar</button>
                                            <button id="eleccion" type="button" class="btn btn-primary" data-bs-dismiss="modal">Elegir este Avatar!</button>
                                        </div>
                                      </div>
                                    </div>
                                  </div>


                            </div>
                            <input id="ImagenElegidaxD" name="img" class="form-control" type="text">
                            <span id="avatarErrors" class="text-danger"><%= (typeof errores != 'undefined' && errores.avatar) ? errores.avatar.msg : "" %></span>
                            <span class="text-danger"  id="submitErrors"></span>
                            
                            <div class="botones my-3">
                                <div class="boton-dos">
                                    <button type="submit" class="mb-3 btn btn-primary">Registrarme</button>
                                </div>
                                
                                <a href="/" class="mb-3 btn btn-danger">Cancelar</a>
                            </div>
                            
                        </form>
                    </div>
                </div>
                
            </main>
            
            <%- include('../partials/scripts') %>
            <script src="/javascripts/seleccionarAvatar.js"></script>
            <script src="/javascripts/register.js"></script> 
            
        </body>
        </html>