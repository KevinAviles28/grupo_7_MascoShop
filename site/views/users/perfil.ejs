<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head') %> 
<body>
  <%- include('../partials/header') %>
  
  <main class="container">
    <h1 class="my-4 text-white bg-dark border-bottom border-warning">Mi perfil</h1> 
    
    
    <img src="/images/users/<%= userNew.avatar %>" class="imagen-perfil" alt="foto-perfil">
    <div class="container-fluid">
      <div class="row">
        <div class="card text-white bg-dark my-3 col-6 ">
          <div class="card-body">
            <p class="card-text border-bottom border-warning">Usuario N° <%= userNew.id %> </p>
            <p class="card-text border-bottom border-warning">Nombre: <%= userNew.username %> </p>
            <p class="card-text border-bottom border-warning">Apellido: <%= userNew.apellido %> </p>
            <p class="card-text border-bottom border-warning">Email: <%= userNew.email %> </p>
          </div>
        </div>
        <div class="card text-white bg-dark my-3 col-6 ">
          <div class="card-body">
            <p class="card-text border-bottom border-warning">Pais: <%= usuario.pais %></p>
            <p class="card-text border-bottom border-warning">Localidad: <%= usuario.localidad %></p>
            <p class="card-text border-bottom border-warning">Telefono: <%= usuario.telefono %></p>
            <p class="card-text border-bottom border-warning">Direccion: <%= usuario.direccion %></p>
          </div>
        </div>
      </div>
    </div>
    <div class="d-flex justify-content-between my-3">
      <a class="btn btn-primary mx-3" href="/users/edit/<%= usuario.id %>">Editar datos</a>
      <form action="/users/delete/<%=userNew.id%>?_method=DELETE" method="POST">
        <button class="btn btn-danger mx-3" type="submit">Eliminar cuenta</button>
      </form>
    </div>
  </main>
  
  
  <%- include('../partials/footer') %>
  <%- include('../partials/scripts') %> 
  
</body>
</html>