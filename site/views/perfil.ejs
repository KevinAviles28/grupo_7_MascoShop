<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %> 
<body>
  <%- include('partials/header') %>
  
<div class="row">
<div class="d-flex justify-content-center mx-0 col-12">
  <div class="card border-dark mb-3" style="max-width: 18rem;">
    <div class="card-body">
    <div class="card-header">Mi perfil</div>
    <div class="card-body">
      <h5 class="card-title">Usuario N° <%=userNew.id%> </h5>
      <p class="card-text">Nombre: <%= userNew.username %> </p>
      <p class="card-text">Apellido: <%= userNew.apellido %> </p>
      <p class="card-text">Email: <%= userNew.email; %> </p>
    </div>
  </div>
  </div>
</div>
<div class="d-flex justify-content-center">
  <form action="/users/delete/<%=userNew.id%>?_method=DELETE" method="POST">
    <button class="btn btn-primary mx-3" type="submit">Eliminar cuenta</button>
  </form>
  
  <a class="btn btn-primary mx-3" href="/users/edit">Editar datos</a>

</div>
</div>
  <a class="btn btn-primary mx-3" href="/users/logout">Cerrar Sessión</a>







  <%- include('partials/footer') %>
  <%- include('partials/scripts') %> 
  
</body>
</html>